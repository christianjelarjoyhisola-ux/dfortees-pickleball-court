# Court Booking Platform

This directory is the canonical Supabase workspace for the clean multi-tenant
platform. The historical files under `../supabase/` are legacy reference only
and must not be applied to new projects.

The development project must be configured through local or Cloudflare runtime
environment variables. Never commit database passwords, service-role keys, or
customer data.

